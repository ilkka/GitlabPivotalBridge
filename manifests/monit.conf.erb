check host <%= @application %> with address 127.0.0.1
        start "/sbin/start gitlabpivotalbridge"
        stop "/sbin/stop gitlabpivotalbridge"
        if failed port <%= @play_port %> protocol HTTP
                request /
                with timeout 5 seconds
                then restart
